<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer glow -->
    <item>
        <shape>
            <solid android:color="@color/button_glow" />
            <corners android:radius="30dp" />
            <stroke android:width="2dp" android:color="@color/button_glow" />
        </shape>
    </item>
    <!-- Inner button -->
    <item android:left="3dp" android:top="3dp" android:right="3dp" android:bottom="3dp">
        <shape>
            <gradient
                android:angle="90"
                android:startColor="@color/button_background"
                android:endColor="@color/button_pressed"
                android:type="linear" />
            <corners android:radius="25dp" />
            <stroke android:width="2dp" android:color="@color/primary_light" />
        </shape>
    </item>
</layer-list>
