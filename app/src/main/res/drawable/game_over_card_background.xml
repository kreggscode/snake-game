<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer glow -->
    <item>
        <shape>
            <gradient
                android:angle="45"
                android:startColor="@color/expert_neon"
                android:endColor="@color/hard_neon"
                android:type="radial" />
            <corners android:radius="20dp" />
        </shape>
    </item>
    <!-- Middle glow -->
    <item android:left="3dp" android:top="3dp" android:right="3dp" android:bottom="3dp">
        <shape>
            <solid android:color="@color/card_glow" />
            <corners android:radius="17dp" />
        </shape>
    </item>
    <!-- Inner card -->
    <item android:left="6dp" android:top="6dp" android:right="6dp" android:bottom="6dp">
        <shape>
            <gradient
                android:angle="90"
                android:startColor="@color/card_background"
                android:endColor="@color/background_dark"
                android:type="linear" />
            <corners android:radius="14dp" />
            <stroke android:width="2dp" android:color="@color/control_border" />
        </shape>
    </item>
</layer-list>
